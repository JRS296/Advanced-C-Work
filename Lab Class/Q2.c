#include <stdio.h>

int main()
{

    int dist;
    float PI = 3.14, area, ci;

    printf("\nEnter Distance between two cities: ");
    scanf("%d", &dist);

    area = PI * rad * rad;
    printf("\nArea of circle : %f ", area);

    ci = 2 * PI * rad;
    printf("\nCircumference : %f ", ci);

    return (0);
}